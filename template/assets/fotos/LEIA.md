# Fotos dos cards

Coloque aqui as fotos usadas nos cards e aponte para elas em `dados/cards.json`, com o caminho `assets/fotos/nome.png`.

Para o layout `recorte`, a foto precisa ser PNG com fundo transparente. Para `disco-var`, foto comum serve, porque o círculo recorta.
